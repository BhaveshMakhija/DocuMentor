import os

def save_temp_file(file, destination: str):
    """
    Saves a requested UploadFile object to a destination.
    Useful for local document ingestion avoiding cloud storage dependencies.
    """
    pass
